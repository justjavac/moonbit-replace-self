import {
  "moonbitlang/core/encoding/utf8",
}

import {
  "moonbitlang/core/env",
  "moonbitlang/x/fs",
} for "test"

supported_targets = "native"

options(
  "native-stub": [ "replace_self_native.c" ],
)
